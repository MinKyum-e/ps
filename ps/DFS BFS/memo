인접 행렬 = 모든 관계를 저장하므로 노드 개수가 많아질수록 메모리 낭비 증가 
인접 리스트 = 연결된 관계만 저장하므로 메모리 적게씀 하지만 두 노드가 연결되어있는지 확인하는데 걸리는 시간이 길다

모든 노드를 방문해야한다 -> 인접 리스트 사용

bfs, dfs 모두 O(N)
일반적으로는 수행시간측면에서 BFS가 DFS보다 좋다 + 구현이 간단하다


         DFS              BFS
동작원리  스택              큐
구현방법  재귀함수           deque